#include <vector>

void swap(std::vector<int>& vec, size_t pos1, size_t pos2);
std::vector<int> bubble_sort(const std::vector<int>& dataset);
std::vector<int> insertion_sort(const std::vector<int>& dataset);
std::vector<int> selection_sort(const std::vector<int>& dataset);
std::vector<int> merge_sort(const std::vector<int>& dataset);